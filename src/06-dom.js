/**
 * Change the text of the p tag on the page with the class change-me to "new text"
 */
function changeTextOnElement() {
    
}

/**
 * When clicking the button with the text "Click me", change the of the button to newButtonText
 *
 * @param {string} newButtonText - The text that the button should have after being clicked
 */
function addButtonClickListener(message) {

}


/**
 * This function changes the background color of an element.
 *
 * @param {Element} element - The DOM element whose background color needs to be changed.
 * @param {string} color - The new background color.
 */
function changeBackgroundColor(element, color) {
    // Your code here

}

